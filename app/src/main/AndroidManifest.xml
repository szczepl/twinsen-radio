<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.TwinsenRadio"
        android:usesCleartextTraffic="true">

        <!-- Deklaracja dla Android Auto: aplikacja typu "media". -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <!-- Motyw uzywany przez Android Auto (kolory naglowka/tla w projekcji). -->
        <meta-data
            android:name="com.google.android.gms.car.application.theme"
            android:resource="@style/CarTheme" />
        <meta-data
            android:name="com.google.android.gms.car.notification.SmallIcon"
            android:resource="@drawable/ic_radio" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.APP_MUSIC" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.NowPlayingActivity"
            android:exported="false"
            android:label="@string/now_playing_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.DiscoverActivity"
            android:exported="false"
            android:label="@string/discover_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.StationInfoActivity"
            android:exported="false"
            android:label="@string/info_title"
            android:parentActivityName=".ui.DiscoverActivity" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".ui.MainActivity" />

        <!--
            Logotypy stacji dla Android Auto. Musi byc exported, bo grafike
            pobiera proces Android Auto, a nie nasz. Nie wystawia niczego poza
            logotypami, ktore i tak siedza w APK.
        -->
        <provider
            android:name=".playback.LogoProvider"
            android:authorities="${applicationId}.logos"
            android:exported="true"
            android:grantUriPermissions="true" />

        <service
            android:name=".playback.RadioService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

    </application>
</manifest>
